findW-find_in_worktree
======================

a specialized alternative for UNIX find(1), which can find &amp; list files or directories in a repository (but skip all submodule trees, Node/NPM install directory trees, etc.)
